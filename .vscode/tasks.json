{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Compile markdown to site",
      "type": "shell",
      "command": "tsc --build; node --enable-source-maps build/scripts/build.js src -o build/public -v",
      "group": "build",
      "presentation": {
        "reveal": "silent",
        "panel": "shared"
      }
    }
  ]
}
